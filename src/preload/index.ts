// Reserved for future contextBridge APIs (e.g. native file dialogs for
// import/export). The renderer currently talks to the app purely over the
// WebSocket server in the main process, so nothing needs to be exposed yet.
